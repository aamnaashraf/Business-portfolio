export type ServiceItem = {
  title: string
  icon: React.ReactNode
  description: string
  features: string[]
  
}

// Add other shared types here as your project grows
export type Testimonial = {
  quote: string
  author: string
  company: string
  avatar?: string
}
